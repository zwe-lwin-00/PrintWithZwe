[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback — Vite/React client-side routes & hash links
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
